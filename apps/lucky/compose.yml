version: "3.3"
services:
  lucky:
    container_name: lucky
    restart: always
    network_mode: host
    environment:
      - PUID=0
      - PGID=0
      - UMASK=022
    volumes:
      - "{{docker_root}}/lucky/luckyconf:/app/conf"
    image: gdy666/lucky:v3
networks: {}
