services:
  libretv:
    container_name: libretv
    restart: always
    network_mode: bridge
    ports:
      - 8080:8080
    environment:
      - PASSWORD={{PASSWORD}}
    image: bestzwei/libretv:latest
